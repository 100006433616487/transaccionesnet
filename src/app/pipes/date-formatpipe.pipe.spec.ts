import { DateFormatpipePipe } from './date-formatpipe.pipe';

describe('DateFormatpipePipe', () => {
  it('create an instance', () => {
    const pipe = new DateFormatpipePipe();
    expect(pipe).toBeTruthy();
  });
});
